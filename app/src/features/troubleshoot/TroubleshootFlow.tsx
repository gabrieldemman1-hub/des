import { Screen } from '../../components/Screen';

/** Placeholder: replaced by the feature build. */
export function TroubleshootFlow() {
  return (
    <Screen title="TroubleshootFlow">
      <p>Being built.</p>
    </Screen>
  );
}
