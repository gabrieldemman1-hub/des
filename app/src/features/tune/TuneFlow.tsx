import { Screen } from '../../components/Screen';

/** Placeholder: replaced by the feature build. */
export function TuneFlow() {
  return (
    <Screen title="TuneFlow">
      <p>Being built.</p>
    </Screen>
  );
}
